import styles from '../styles/Footer.module.css'

export default function Footer() {
    return(
        <div id={styles.footer}>
            <p id={styles.footertext}>whats the web without a good design?</p>
        </div>
    )
}